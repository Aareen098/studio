
{
  "entities": {
    "HiringRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "HiringRequest",
      "type": "object",
      "description": "Represents a hiring inquiry from the portfolio website.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the hiring request."
        },
        "name": {
          "type": "string",
          "description": "The name of the person submitting the inquiry."
        },
        "email": {
          "type": "string",
          "description": "The email address of the person submitting the inquiry.",
          "format": "email"
        },
        "company": {
          "type": "string",
          "description": "The company name of the person."
        },
        "projectType": {
          "type": "string",
          "description": "The type of project the person is interested in."
        },
        "budget": {
          "type": "string",
          "description": "The estimated budget for the project."
        },
        "message": {
          "type": "string",
          "description": "The detailed project description from the form."
        },
        "submissionDate": {
          "type": "string",
          "description": "The date and time the form was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "projectType",
        "budget",
        "message",
        "submissionDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/hiringRequests/{hiringRequestId}",
        "definition": {
          "entityName": "HiringRequest",
          "schema": {
            "$ref": "#/backend/entities/HiringRequest"
          },
          "description": "Stores hiring inquiries from the portfolio website.",
          "params": [
            {
              "name": "hiringRequestId",
              "description": "Unique identifier for the hiring request."
            }
          ]
        }
      }
    ]
  }
}
